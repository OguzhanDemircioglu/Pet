/**
 * Public request DTOs for the order module — accessible from other modules
 * (notably payment) for forwarding HTTP request bodies into order/payment flows.
 */
@org.springframework.modulith.NamedInterface("dto-request")
package com.petshop.order.dto.request;
